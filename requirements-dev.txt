# Test + lint dependencies for CI and local development.
# Production code dependencies live with each sub-project (e.g. mcp_massive-master/pyproject.toml,
# Options Quant Trading/.env.local.example).

pytest>=7.4,<9
numpy>=1.24
pandas>=2.0
scipy>=1.10

# LevFin deliverable tie-out tests read the .docx, .pptx and .xlsx artefacts.
openpyxl>=3.1
python-docx>=1.1
python-pptx>=0.6.21

flake8>=6
